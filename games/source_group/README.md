# Group mode levels (Laser Trap)

Put Group playlist `.led` files in the dash folders (`----`, `-`, `--`, `---`).
Order within a folder = filename sort (same as 1P).
Admins can replace these files without touching `games/source/`.
Initial seed = first 3 levels from each matching 1P tier.
